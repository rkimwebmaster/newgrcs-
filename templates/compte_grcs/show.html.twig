{% extends 'base.html.twig' %}

{% block title %}CompteGRCS{% endblock %}
{% block ariane %}{{parent() }} Comptes GRCS{% endblock %}
{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>QuantiteDiesel</th>
                <td>{{ compte_g_r_c.quantiteDiesel }}</td>
            </tr>
            <tr>
                <th>QuantiteEssence</th>
                <td>{{ compte_g_r_c.quantiteEssence }}</td>
            </tr>
            <tr>
                <th>DateDernierApprovisionnement</th>
                <td>{{ compte_g_r_c.dateDernierApprovisionnement ? compte_g_r_c.dateDernierApprovisionnement|format_datetime('long') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compte_g_r_c_s_index') }}">retour à la liste</a>

    <a href="{{ path('app_compte_g_r_c_s_edit', {'id': compte_g_r_c.id}) }}"><span class="fa fa-edit" /></a>

    {{ include('compte_grcs/_delete_form.html.twig') }}
{% endblock %}

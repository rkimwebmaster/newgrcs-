{% extends 'base.html.twig' %}

{% block title %}MessagePetitClient{% endblock %}
{% block ariane %}
{{parent() }} Message Petit client 
{% endblock %}
{% block body %}
    <table class="table">
        <tbody>
            
            <tr>
                <th>Date</th>
                <td>{{ message_petit_client.date ? message_petit_client.date|format_datetime('long') : '' }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ message_petit_client.sujet }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ message_petit_client.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_message_petit_client_index') }}">retour à la liste</a>

    <a href="{{ path('app_message_petit_client_edit', {'id': message_petit_client.id}) }}"><span class="fa fa-edit" /></a>

    {{ include('message_petit_client/_delete_form.html.twig') }}
{% endblock %}

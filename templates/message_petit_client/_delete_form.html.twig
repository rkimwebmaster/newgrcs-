<form method="post" action="{{ path('app_message_petit_client_delete', {'id': message_petit_client.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer définitivement cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message_petit_client.id) }}">
    <button class="btn btn-default "> <span class="fa fa-times-circle " > Supprimer  </button>
</form>

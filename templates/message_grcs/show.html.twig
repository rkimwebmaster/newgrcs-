{% extends 'base.html.twig' %}

{% block title %}MessageGRCS{% endblock %}
{% block ariane %}
{{parent() }} Message GRCS
{% endblock %}
{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Date</th>
                <td>{{ message_g_r_c.date ? message_g_r_c.date|format_datetime('long') : '' }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ message_g_r_c.sujet }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ message_g_r_c.contenu }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('app_message_g_r_c_s_index') }}">retour à la liste</a> #}

    {# <a href="{{ path('app_message_g_r_c_s_edit', {'id': message_g_r_c.id}) }}"><span class="fa fa-edit" /></a> #}

    {# {{ include('message_grcs/_delete_form.html.twig') }} #}
{% endblock %}

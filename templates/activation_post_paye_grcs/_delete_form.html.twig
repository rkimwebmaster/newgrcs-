<form method="post" action="{{ path('app_activation_post_paye_g_r_c_s_delete', {'id': activation_post_paye_g_r_c.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activation_post_paye_g_r_c.id) }}">
    <button class="btn">Delete</button>
</form>

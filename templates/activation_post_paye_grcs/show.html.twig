{% extends 'base.html.twig' %}

{% block title %}ActivationPostPayeGRCS{% endblock %}
{% block lesboutons %}
	<a type="button" title="Ajouter nouveau "  href="{{path('app_grand_fournisseur_index')}}"class="btn btn-sm btn-info">
		<i class="fas fa-reply-all"></i>
		Liste 
	</a>
{% endblock %}
{% block ariane %}
	GRCS
	<span class="fa fa-angle-double-right"></span>
	Mode PostPayé
{% endblock %}
{% block body %}
<table id="example1" class="table table-striped table-hover ">
        <tbody>
            <tr>
                <th>Date Debut</th>
                <td>{{ activation_post_paye_g_r_c.dateDebut ? activation_post_paye_g_r_c.dateDebut|format_datetime('long') : '' }}</td>
            </tr>
            <tr>
                <th>Date Fin</th>
                <td>{{ activation_post_paye_g_r_c.dateFin ? activation_post_paye_g_r_c.dateFin|format_datetime('long') : '' }}</td>
            </tr>
            <tr>
                <th>Durée</th>
                <td>{{ activation_post_paye_g_r_c.nombreJour ? activation_post_paye_g_r_c.nombreJour.format('%y year(s), %m month(s), %d day(s)') : '' }}</td>
            </tr>
            <tr>
                <th>Is Cloture</th>
                <td>{{ activation_post_paye_g_r_c.isCloture ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Quantite Max Diesel Autorise</th>
                <td>{{ activation_post_paye_g_r_c.quantiteMaxDieselAutorise }}</td>
            </tr>
            <tr>
                <th>Quantite Max Essence Autorise</th>
                <td>{{ activation_post_paye_g_r_c.quantiteMaxEssenceAutorise }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('app_activation_post_paye_g_r_c_s_index') }}">back to list</a> #}

    {# <a href="{{ path('app_activation_post_paye_g_r_c_s_edit', {'id': activation_post_paye_g_r_c.id}) }}">edit</a> #}

    {# {{ include('activation_post_paye_grcs/_delete_form.html.twig') }} #}
{% endblock %}

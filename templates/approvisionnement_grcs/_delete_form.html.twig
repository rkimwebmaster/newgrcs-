<form method="post" action="{{ path('app_approvisionnement_g_r_c_s_delete', {'id': approvisionnement_g_r_c.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer définitivement cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ approvisionnement_g_r_c.id) }}">
    <button class="btn btn-default "> <span class="fa fa-times-circle " > Supprimer  </button>
</form>

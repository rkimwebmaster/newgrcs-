{% extends 'base.html.twig' %}

{% block title %}GRCS index{% endblock %}
{% block ariane %}
{{parent() }} GRCS
{% endblock %}
{% block body %}
    <table id="example1" class="table table-striped table-hover ">
        <thead>
            <tr>
                <th>#</th>
                <th>Identité </th>
                <th>Adresse</th>
            </tr>
        </thead>
        <tfoot>
            <tr>
                <th>#</th>
                <th>Identité </th>
                <th>Adresse</th>
            </tr>
        </tfoot>
        <tbody>
        {% for g_r_c in g_r_cs %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ grand_fournisseur.identite }}</td>
                <td>{{ grand_fournisseur.adresse }}</td>
                <td>
                    <a href="{{ path('app_g_r_c_s_show', {'id': g_r_c.id}) }}"><span class="fa fa-search-plus" /></a>
                    <a href="{{ path('app_g_r_c_s_edit', {'id': g_r_c.id}) }}"><span class="fa fa-edit" /></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2"class="text-warning">aucun enregistrement dans le système.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('app_g_r_c_s_new') }}"class="btn btn-default "> <span class="fa fa-plus-circle"> Créer nouveau </a> #}
{% endblock %}
